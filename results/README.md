# Results directory

No corrected empirical results exist yet. The aggregate values discussed in the
top-level README are historical reproduced values from the audited doubled
pipeline, not outputs of this repository.

Future corrected outputs should be generated only after every validation gate
in `docs/reproducibility.md` passes. Large predictions, metadata, features,
models, and raw data are ignored by Git and must not be redistributed here.
